import React from 'react';

import './article.scss';

const Article = ({ imgUrl, date, text, designation, link }) => (
  <section className="tf-section article">
  </section>
);

export default Article;
